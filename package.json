{
  "name": "rdvp",
  "version": "0.1.0",
  "private": true,
  "description": "RDVP workspace",
  "scripts": {
    "spring-backend:test": "cd rdvp-backend && mvnw.cmd test",
    "spring-backend:start": "cd rdvp-backend && mvnw.cmd spring-boot:run"
  }
}
