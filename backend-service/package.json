{
  "name": "@rdvp/backend-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "RDVP backend service",
  "engines": {
    "node": ">=18.20.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/test/*.test.js",
    "start": "node dist/src/main.js"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.0"
  }
}
